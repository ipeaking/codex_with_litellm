[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-client"
version = "0.1.0"
description = "Internal AI client for the enterprise AI incident course."
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
